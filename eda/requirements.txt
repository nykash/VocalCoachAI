# For progress bars (optional; script still runs without it)
tqdm>=4.60.0

# Breath support (eda/breath_support.py)
librosa>=0.10.0
numpy>=1.20.0

# Optional: more accurate HNR/jitter (Praat backend)
# parselmouth>=0.4.0

# Optional: live microphone input
# sounddevice>=0.4.0

# Optional: load MP4 via pydub (if librosa fails on mp4)
# pydub>=1.0.0

# Real-time breath support web app (eda/breath_app)
flask>=2.0.0

# VAE singer / artist similarity (eda/train_vae_singer.py, artist_similarity_vae.py)
torch>=2.0.0
lightning>=2.0.0
scikit-learn>=1.0.0
matplotlib>=3.5.0
seaborn>=0.12.0
pandas>=1.3.0
